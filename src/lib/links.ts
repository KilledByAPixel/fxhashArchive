/**
 * Where this project lives.
 *
 * One constant because the repository was renamed once already (fxhashViewer ->
 * fxhashArchive), and the copies of this URL scattered through components and
 * their tests are exactly what a rename leaves behind, silently pointing at
 * nothing.
 */
export const REPO_URL = 'https://github.com/KilledByAPixel/fxhashArchive'
